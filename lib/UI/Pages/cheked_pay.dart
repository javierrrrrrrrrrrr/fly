import 'package:flutter/material.dart';

class CheckPay extends StatelessWidget {
  const CheckPay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
