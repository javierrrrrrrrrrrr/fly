import 'package:flutter/material.dart';

class CradBody5 extends StatelessWidget {
  const CradBody5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("Card body5"),
    );
  }
}
