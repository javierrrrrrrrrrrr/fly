import 'package:flutter/material.dart';

class CradBody3 extends StatelessWidget {
  const CradBody3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("Card body3"),
    );
  }
}
