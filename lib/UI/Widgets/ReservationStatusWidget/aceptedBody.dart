import 'package:flutter/material.dart';

class AceptedBody extends StatelessWidget {
  const AceptedBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text("Acepted"),
      ),
    );
  }
}
